let calender = ""

const calenderURL =

  /*
  Calender.onshow = function() {
    getData = () =>
      fetch(calenderURL).then(response => response.json()).then(({
        results
      }) => results)

    function freeData(apiData) {
      calender = apiData
    }

    getData().then(data => {
      freeData(data, calender)
      calender = data
    })

  }
  */
  
Canvas.onshow=function(){
  //Label1.value = weather.days[0].temp
  //console.log(`the temp is ${weather.days[0].temp}`)
  }
  
  HamburgerC.onclick=function(s){
  if (typeof(s) == "object")
       return
     else {
       switch(s) {
            case "Homepage":
                ChangeForm(Homepage)
                break
                   case "Canvas":
                ChangeForm(Canvas)
                break
                    case "Shopping List":
                ChangeForm(shoppingList)
                break
                  case "Weather":
                ChangeForm(Weather)
                break
                  case "GPA Calculator":
                ChangeForm(gpaCalc)
                break
       }  
}